from app import db

# Create your models here
# Don't forget to create a database before runnning the syncdb script

#Example: 
# class Foo(db.Model):
    # id = db.Column(db.Integer, autoincrement=True, nullable=False, unique=True)
